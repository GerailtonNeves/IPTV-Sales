services:
  - type: web
    name: iptv-sales-bot
    env: node
    plan: free
    buildCommand: npm install --no-audit --no-fund
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 20.12.0
